/*
Clueless Cats: Vansh saboo + Tiffany, Lauren Lee + Bo, Diana Akhmedova + Ajax
APCS
HW 18: CPA-One
2021-10-14

DISCO:
QCC: Are constructors methods? Would constructors be considered methods in a UML diagram?
Clueless Cats's Latest and Greatest Q2 Response:
Clueless Cats's Latest and Greatest Q3 Response:
*/

public class BankAccount {
  private String name;
  private String password;
  private int pin;
  private int accountNum;
  private float balance;

  //setting each attribute
  public static void setName(String fullName) {
    name = fullName;
  }
  public static void setName(String fullName) {
    name = fullName;
  }

  public BankAccount () {
    name = "John Doe";
    password = "password";
    pin = 1234;

    Random random = new Random ();
    accountNum = random.nextInt(1000, 10000);

    balance = 0;
  }

  public BankAccount (String firstLast, String newPass, int newPin) {
    name = firstLast;
    password = newPass;
    pin = newPin;

    Random random = new Random ();
    accountNum = random.nextInt(1000, 10000);

    balance = 0;
  }

  public static String printinfo (int account, String pass){
    if (account == accountNum && pass = password){
      return (name + "\n" + password + "\n" + (str) pin + "\n" + accountNum + "\n" + balance);
    }else{
      return ("Your password or account number is wrong.");
    }
  }

  public static float deposit (float amtDeposit) {
    balance = balance + amtDeposit;
    return balance;
  }

  public static float withdraw (float amtWithdraw) {
    balance = balance - amtWithdraw;
    return balance;
  }

  public static void main (String[] args) {
    BankAccount John = newBank();

    String info;
    info = John.printInfo(accountNum, password);

    System.out.println(info);

    String dep;
    dep = John.deposit(50);

    System.out.println(dep);
  }
}
